# Models (planned)

This package will host data classes for `Cell` and `Notebook` after the MVP is functional.

- Cell: id, type, content, metadata, outputs, timestamps
- Notebook: id, title, ordered cell_ids, metadata, timestamps

Planned for Phase 1–2 post-MVP stabilization.
